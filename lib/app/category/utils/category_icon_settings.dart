import 'package:flutter/material.dart';

class CategoryIconSettings {
  final IconData icon;
  final Color color;

  const CategoryIconSettings({
    required this.icon,
    required this.color
  });
}